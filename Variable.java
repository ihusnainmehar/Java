
public class Variable {
    public static void main(String[] args) {
        // variables
        int a = 20; // Use to store integer
        String name = "Tony Stark"; // Use to store Text
        Double pin = 20.00021234411111111; // Use to store Decimal with large size
        byte bit = 120; //only store valuse less than or equal to q byte
        short sh = 16000; //use to store short integers
        //Float ge = 20.0002111; // Use to store Decimal upto 7 decimal points
        long lg = 1374947597; //Use to store long interger valuse
        char character = 'C'; //only stores single character
        Boolean bool; //Give only two outputs 0,1 or true,false
    }
}
